{
  "name": "atlas",
  "type": "module",
  "version": "1.0.0",
  "description": "atlas AI assistant monorepo",
  "scripts": {
    "prestart": "docker compose up -d",
    "start": "run-p start:*",
    "start:next": "cd next && npm run dev",
    "start:api": "cd api && npm start",
    "make-migration": "npx typeorm-ts-node-commonjs migration:generate -d ./api/src/data-source.ts ./api/src/migration/migration",
    "make-cert": "./makecert.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "cece wren",
  "license": "UNLICENSED",
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}

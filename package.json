{
  "name": "atlas",
  "type": "module",
  "version": "1.0.0",
  "description": "atlas AI monorepo",
  "scripts": {
    "prestart": "docker compose up -d",
    "reset": "docker compose down --volumes",
    "start": "run-p start:*",
    "start:next": "cd next && npm run dev",
    "start:api": "cd api && npm start",
    "make-migration": "npx typeorm-ts-node-commonjs migration:generate -d ./api/src/data-source.ts ./api/src/migration/migration",
    "make-cert": "./make-cert.sh",
    "make-hosts": "sudo hostile set 127.0.0.1 local.atlasai.zone",
    "setup": "npm run make-cert && npm run make-hosts",
    "test": "run-s test:*",
    "test:next": "cd next && npm test",
    "test:api": "cd api && npm test"
  },
  "author": "cece wren <cecelia.wren@gmail.com>",
  "license": "UNLICENSED",
  "devDependencies": {
    "hostile": "^1.3.3",
    "npm-run-all": "^4.1.5"
  }
}
